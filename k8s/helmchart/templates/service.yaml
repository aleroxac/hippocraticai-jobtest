apiVersion: v1
kind: Service
metadata:
  name: {{ include "hippocraticai-api.fullname" . }}
  labels:
    {{- include "hippocraticai-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hippocraticai-api.selectorLabels" . | nindent 4 }}
